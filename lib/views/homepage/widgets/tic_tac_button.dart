import 'package:flutter/material.dart';

class TicTacButton extends StatelessWidget {
  const TicTacButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
